# Editors
*.swp
*.swo
*~
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Vergil
.venv/
.worktrees/
.vergil/
.superpowers/
build/

# vrg-validate artifacts — regenerated by every run, never committed
.coverage
coverage.xml
junit.xml
licenses.json
pip-audit.json

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/

# Melete output — generated practice sheets are not source
sessions/

# melete-render (Node/alphaTab) — the renderer's deps are baked into the
# container image via [container].build-command (global install), never
# vendored here; a local node_modules would be masked by the runtime mount.
melete-render/node_modules/
